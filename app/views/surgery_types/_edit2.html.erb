<%= form_with(model: @surgery_recipe_req, local: true) do |form| %>
<div class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header" data-background-color="purple">
            <h4 class="title">Edit Surgery Recipe</h4>
            <p class="category"></p>
          </div>
          <div class="card-content table-responsive">
            <table class="table table-hover">
              <thead class="text-primary">
                <tr>
                  <th>Item Name</th>
                  <th>Qty Used</th>
                  <th>Submit</th>
                </tr>
              </thead>

              <tbody>
                <div class="col-md-12">
                  <div class="form-group label-floating is-empty">
                    <% @surgery_type.my_reqs.each do |req| %>
                      <tr>
                          <label class="control-label"><%#= form.label :surgery_type_id %></label>
                          <td><%= req.supply_list.item_name %></td>
                          <td><%= form.number_field :qty , value: req.qty%></td>
                          <!-- <td><%#= req.text_field :qty , value: req.qty%></td> -->
                        <% end %>
                      </tr>
                      <td><%= submit_tag "Update", class: 'btn btn-primary' %></td>

                    <% end %>
                  </div>
                </div>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- form ajax form remote true ajax load remote true -->
<!-- , data: { remote: true } -->
<!-- data: { remote: true }, method: 'put', -->
