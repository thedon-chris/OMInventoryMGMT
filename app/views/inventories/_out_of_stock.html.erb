<table>
<thead>
  <tr>
    <th>Clinic</th>
    <th>Supply Item</th>
    <th>Qty</th>
    <th>Out of Stock</th>
    <th colspan="3"></th>
  </tr>
</thead>

<tbody>
    <% @inventories.each do |inventory| %>
    <% if inventory.out_of_stock %>
      <tr>
        <td><%= inventory.clinic.clinic_name %></td>
        <td><%= link_to inventory.supply_list.item_name, inventory %></td>
        <td><%= inventory.qty %></td>
        <td><%= inventory.out_of_stock %></td>
        <td><%= link_to 'Delete', inventory, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    <% end %>
</tbody>
</table>
